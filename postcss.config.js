// 导出一个默认对象，通常用于配置文件或模块
export default {
  // 定义一个名为plugins的对象，用于存放各种插件配置
  plugins: {
    // 配置tailwindcss插件，用于样式处理
    tailwindcss: {},
    // 配置autoprefixer插件，用于自动添加浏览器前缀，确保CSS兼容性
    autoprefixer: {},
  },
}
